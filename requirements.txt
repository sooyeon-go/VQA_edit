# Pipeline + HunyuanImage-3.0-Instruct (see SETUP.txt for PyTorch install order)
einops==0.8.1
numpy==2.2.0
pillow==12.0.0
diffusers==0.35.2
safetensors==0.7.0
tokenizers==0.22.0
transformers[accelerate,tiktoken]==4.57.1
huggingface_hub[cli]>=1.0.0
loguru>=0.7.3
accelerate>=1.0.0
qwen-vl-utils>=0.0.8

# Optional (uncomment in setup_env.sh)
# flashinfer-python==0.5.0
# flash-attn==2.8.3
